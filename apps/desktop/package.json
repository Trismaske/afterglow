{
  "name": "afterglow-desktop",
  "productName": "Afterglow",
  "version": "0.1.0",
  "private": true,
  "author": "Tristan <trismaske@gmail.com>",
  "description": "Fullscreen ambient photo display with intelligent ordering and flag-to-queue capture.",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && electron .",
    "test": "vitest run",
    "dist": "npm run build && electron-builder"
  },
  "dependencies": {
    "@afterglow/core": "*",
    "exifr": "^7.1.3"
  },
  "devDependencies": {
    "electron": "^37.0.0",
    "electron-builder": "^26.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
